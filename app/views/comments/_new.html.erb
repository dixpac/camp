<%= form_with(model: comment, url: bucket_recording_comments_url(@bucket, parent), local: true) do |form| %>
  <div class="field">
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
